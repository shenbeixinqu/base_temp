const state = () => ({
  routes: []
})

const getters = {
  routes: (state) =>  state.routes
}

const mutations = {

}

const actions = {

}

export default { state, getters, mutations, actions }